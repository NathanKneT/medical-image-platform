DATABASE_URL=postgresql+asyncpg://medical_user:medical_pass@db:5432/medical_platform

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:8080

# File Upload Settings
MAX_FILE_SIZE=52428800  # 50MB in bytes
ALLOWED_EXTENSIONS=.jpg,.jpeg,.png,.dcm,.nii,.nii.gz
UPLOAD_DIR=uploads

# Security Settings
SECRET_KEY=development-secret-key-change-in-production-to-32-char-random-string

# Processing Settings (for demo)
MIN_PROCESSING_TIME=5   # seconds
MAX_PROCESSING_TIME=20  # seconds

# WebSocket Settings
WS_HEARTBEAT_INTERVAL=30  # seconds

# Redis
REDIS_URL=redis://redis:6379/DATABASE_URL=postgresql+asyncpg://medical_user:medical_pass@db:5432/medical_platform

# CORS Configuration
CORS_ORIGINS='["http://localhost:3000", "http://localhost:3001", "http://localhost:8080"]'

# File Upload Settings
MAX_FILE_SIZE=52428800  # 50MB in bytes
ALLOWED_EXTENSIONS='[".jpg", ".jpeg", ".png", ".dcm", ".nii", ".nii.gz"]'
UPLOAD_DIR=uploads

# Security Settings
SECRET_KEY=development-secret-key-change-in-production-to-32-char-random-string

# Processing Settings (for demo)
MIN_PROCESSING_TIME=5   # seconds
MAX_PROCESSING_TIME=20  # seconds

# WebSocket Settings
WS_HEARTBEAT_INTERVAL=30  # seconds

# Redis
REDIS_URL=redis://redis:6379/0
